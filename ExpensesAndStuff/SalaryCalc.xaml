<Window
    x:Class="ExpensesAndStuff.SalaryCalc"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ExpensesAndStuff"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SalaryCalc"
    Width="400"
    Height="450"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="20">
        <StackPanel>
            <!--  Dropdown  -->
            <ComboBox ItemsSource="{Binding CalculationModes}" SelectedItem="{Binding SelectedMode}" />

            <!--  Hourly Wage Mode  -->
            <StackPanel Visibility="{Binding IsHourlyMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock>Enter yearly hourcount</TextBlock>
                <TextBox Text="{Binding YearlyHours}" />
                <TextBlock>Enter hourly wage</TextBlock>
                <TextBox Text="{Binding HourlyWage}" />
            </StackPanel>

            <!--  Yearly Salary Mode  -->
            <StackPanel Visibility="{Binding IsYearlyMode, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock>Enter Yearly Salary</TextBlock>
                <TextBox Text="{Binding YearlySalary}" />
            </StackPanel>

            <!--  Calculate Button  -->
            <Button Command="{Binding CalculateCommand}" Content="Calculate Salary!" />

            <!--  Result  -->
            <TextBlock
                FontSize="16"
                FontWeight="Bold"
                Text="{Binding MonthlySalary}" />


        </StackPanel>
    </Grid>
</Window>
